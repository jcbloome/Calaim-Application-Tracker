
'use client';

// This is a wrapper around the main consolidated waivers form page component.
// It ensures that the form page is loaded within the admin layout.
import WaiversPage from '@/app/forms/waivers/page';

export default function AdminWaiversPage() {
    return <WaiversPage />;
}
