
'use client';

// This is a wrapper around the main Freedom of Choice form page component.
// It ensures that the form page is loaded within the admin layout.
import FreedomOfChoicePage from '@/app/forms/freedom-of-choice/page';

export default function AdminFreedomOfChoicePage() {
    return <FreedomOfChoicePage />;
}
