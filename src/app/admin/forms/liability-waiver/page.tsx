
'use client';

// This is a wrapper around the main Liability Waiver form page component.
// It ensures that the form page is loaded within the admin layout.
import LiabilityWaiverPage from '@/app/forms/liability-waiver/page';

export default function AdminLiabilityWaiverPage() {
    return <LiabilityWaiverPage />;
}
