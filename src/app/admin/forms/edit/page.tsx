
'use client';

// This is essentially a wrapper around the main CS Summary Form component
// It ensures that the form is loaded within the admin layout.
import CsSummaryFormPage from '@/app/forms/cs-summary-form/page';

export default function AdminEditFormPage() {
    return <CsSummaryFormPage />;
}
