rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allows anyone logged in (like jason@carehomefinders.com) to access the data
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}